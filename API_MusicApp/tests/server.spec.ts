//import { SERVER } from "../src/index";
import { expect } from "chai";
import request from "supertest";
import 'mocha';

describe("Server Correct Function", ()=>{
    it("Test EndPoint 1",(done)=>{
        /* request(SERVER)
            .get('/')
            .expect(200, done); */
    });

});